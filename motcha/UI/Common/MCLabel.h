#import <UIKit/UIKit.h>

/*
 * MCLabel is a subclass of UILabel, which will adjust its
 * height according to the string length in the label view,
 * based on its frame width as its preferredMaxLayoutWidth.
 */

@interface MCLabel : UILabel
@end
